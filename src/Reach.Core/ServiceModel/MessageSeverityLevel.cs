namespace Reach.ServiceModel
{
    /// <summary>
    /// Commonly accepted severity levels used to categorize messages.
    /// </summary>
    public enum MessageSeverityLevel
    {
        Debug,
        Info,
        Warning,
        Error
    }
}
